C# comes from the C family of languages (C, C++, C#). The syntax is similar in the sense that its the same use of brazes, control statements, and looping constructs. 

Hello world in C#:
```cs
// Comment
Console.WriteLine("Hello, World")
```

The WriteLine method of the Console class, which is in the **System** namespace, produces output of the program. 

Alternative format:
```cs
using System;
namespace TourOfCsharp

class Program
{
	static void Main()
	{
		// Comment
		Console.WriteLine("Hello, World")
	}
}
```

